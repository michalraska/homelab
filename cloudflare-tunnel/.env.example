# ==================== Cloudflare Tunnel Configuration ====================
# Note: TZ (timezone) is inherited from root .env file

# Cloudflare Tunnel token for external access
# How to create:
#   1. Install cloudflared: https://developers.cloudflare.com/cloudflare-one/connections/connect-applications/install-and-setup/installation/
#   2. Run: cloudflared tunnel create homelab
#   3. Run: cloudflared tunnel token homelab
#   4. Copy the generated token here
CLOUDFLARE_TUNNEL_TOKEN=your_cloudflare_tunnel_token_here
