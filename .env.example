# ==================== HOMELAB ENVIRONMENT CONFIGURATION ====================
#
# This file contains the root-level environment variables used by all services.
# Service-specific variables are in each service directory's .env file.
#
# Directory Structure:
#   compose.yaml              ← Root compose file (networks + service includes)
#   traefik/
#     ├── .env                ← Traefik variables (ACME_EMAIL, CLOUDFLARE_API_KEY)
#     ├── .env.example        ← Traefik template
#     ├── compose.yaml        ← Traefik service definition
#     └── data/               ← Traefik certificates & acme.json
#   cloudflare-tunnel/
#     ├── .env                ← Cloudflare Tunnel variables (CLOUDFLARE_TUNNEL_TOKEN)
#     ├── .env.example        ← Cloudflare Tunnel template
#     ├── compose.yaml        ← Cloudflare Tunnel service definition
#     └── data/               ← Tunnel credentials storage
#   immich/
#     ├── .env                ← Immich-specific variables
#     ├── .env.example        ← Immich template
#     ├── compose.yaml        ← Immich service definition
#     ├── library/            ← Immich photo uploads
#     └── db/                 ← PostgreSQL database
#   adguard/
#     ├── .env                ← AdGuard-specific variables (none required)
#     ├── .env.example        ← AdGuard template
#     ├── compose.yaml        ← AdGuard service definition
#     ├── work/               ← AdGuard working directory
#     └── conf/               ← AdGuard configuration
#   arr/
#     ├── .env                ← ARR stack variables
#     ├── .env.example        ← ARR stack template
#     ├── compose.yaml        ← Media management services (Sonarr, Radarr, etc.)
#     └── appdata/            ← Service configurations
#   dashdot/
#     ├── .env                ← Dashdot variables (DOMAIN)
#     ├── .env.example        ← Dashdot template
#     └── compose.yaml        ← Dashdot service definition
#
# Networks:
#   ingress      ← External access (Traefik, Cloudflare Tunnel)
#   services     ← Internal services (Traefik, Immich, AdGuard, arr)
#   immich       ← Immich-isolated (Immich services only)
#   vpn-isolated ← VPN-isolated (qBittorrent via Gluetun)
#
# ==================== ROOT ENVIRONMENT VARIABLES ====================

# System timezone - inherited by all services
# Find your timezone: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
# Common examples: Europe/Prague, UTC, America/New_York, Asia/Tokyo
TZ=Europe/Prague

# ==================== SERVICE-SPECIFIC CONFIGURATION ====================
# These variables are configured in their respective service directories:
#
# Cloudflare Tunnel (cloudflare-tunnel/.env):
#   - CLOUDFLARE_TUNNEL_TOKEN: Token for external tunnel access
#
# Traefik (traefik/.env):
#   - ACME_EMAIL: Email for Let's Encrypt notifications
#   - CLOUDFLARE_API_KEY: Scoped API token for DNS-01 challenge (Zone > DNS > Edit + Zone > Zone > Read)
#
# Immich (immich/.env):
#   - DOMAIN: Your domain name (e.g., yourdomain.com)
#   - DB_PASSWORD: Strong PostgreSQL password
#   - DB_USERNAME: PostgreSQL username (default: immich)
#   - DB_DATABASE_NAME: PostgreSQL database name (default: immich)
#   - IMMICH_VERSION: Docker image tag (default: release)
#
# AdGuard (adguard/.env):
#   - No service-specific variables required
#
# ARR Stack (arr/.env):
#   - PUID/PGID: User/group IDs for file ownership
#   - OPENVPN_USER: NordVPN service username
#   - OPENVPN_PASSWORD: NordVPN service password
#   - SERVER_COUNTRIES: VPN server location (e.g., "Czech Republic")
#   - DOMAIN: Your domain for accessing arr services
#
# Dashdot (dashdot/.env):
#   - DOMAIN: Your domain for accessing Dashdot
#
# See the respective .env.example files in each service directory for full details.
