# ==================== Immich Service Configuration ====================
# Documentation: https://docs.immich.app/install/environment-variables

# Domain for Traefik routing (required for compose file variable substitution)
DOMAIN=yourdomain.com

# Storage locations
UPLOAD_LOCATION=./library
DB_DATA_LOCATION=./postgres

# Immich version tag (release = stable)
IMMICH_VERSION=release

# Database credentials
# IMPORTANT: Use only alphanumeric characters (A-Za-z0-9) for password
DB_USERNAME=postgres
DB_DATABASE_NAME=immich
DB_PASSWORD=postgres
